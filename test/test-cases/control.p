//&S-
//&T-

control;

begin
  var debug: boolean;
  var counter: integer;
  
  // if
  if (1 > 0) then
    print true;
  else
    print false;
  end if

  if debug = true then
    print "[LOG]\n";
  end if

  // while
  counter := 0;
  while counter < 10 do
    print "Taiwan No.1\n";
  end do

  // for
  for idx := 0 to 10 do
    begin
      var idx: 10;
      print "ISA (Ideology State Automaton)\n";
    end
  end do

  for idx := 0 to 10 do
  end do

  return 11;
end
end control
